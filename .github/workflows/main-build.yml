name: "Main Branch Build (deprecated)"

# Deprecated for OpenFlow (milestone M5). The cross-platform build was
# consolidated into the self-contained matrix workflow in `build.yml`
# (triggers: manual dispatch + `v*` tags). This file is kept as a manual
# no-op so no stale reference to the old reusable workflow remains.

on: workflow_dispatch

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Consolidated into build.yml — run the 'Build' workflow instead."
