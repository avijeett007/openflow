name: "Release (deprecated)"

# Deprecated for OpenFlow (milestone M5). Releases are now produced by pushing
# a `v*` tag, which triggers `build.yml` to build and attach installers to a
# draft GitHub Release. This file is kept as a manual no-op.

on: workflow_dispatch

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Deprecated — push a v* tag to trigger a release via build.yml."
