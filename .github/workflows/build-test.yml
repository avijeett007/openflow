name: "Build Test (deprecated)"

# Deprecated for OpenFlow (milestone M5). Use the self-contained `build.yml`
# workflow (manual dispatch) for test builds. Kept as a manual no-op.

on: workflow_dispatch

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: echo "Consolidated into build.yml — run the 'Build' workflow instead."
